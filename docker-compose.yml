version: '3.0'

services:
  trash-alarm-dev-env:
    build: .
    container_name: trash-alarm-dev-env
    volumes:
      - ./:/home/trash-alarm
      - /dev:/dev
    privileged: true
    working_dir: /home/trash-alarm
    command: sudo /usr/bin/supervisord
    ports:
      - 2222:22
